name: CI/CD ms-shipping

on:
  workflow_dispatch:
  push:
    branches: [deploy]
    paths:
      - 'ms-shipping/**'
      - '.github/workflows/ms-shipping.yml'

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Credenciales AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build y push imagen
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $REGISTRY/amazon-ms-shipping:$IMAGE_TAG -t $REGISTRY/amazon-ms-shipping:latest ./ms-shipping
          docker push $REGISTRY/amazon-ms-shipping:$IMAGE_TAG
          docker push $REGISTRY/amazon-ms-shipping:latest

      - name: Instalar kubectl
        run: |
          curl -LO "https://dl.k8s.io/release/v1.29.0/bin/linux/amd64/kubectl"
          sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl

      - name: Configurar kubectl para EKS
        run: |
          aws eks update-kubeconfig --name ${{ secrets.EKS_CLUSTER_NAME }} --region ${{ secrets.AWS_REGION }}

      - name: Desplegar en EKS
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          kubectl set image deployment/ms-shipping ms-shipping=$REGISTRY/amazon-ms-shipping:$IMAGE_TAG -n amazon
          kubectl rollout status deployment/ms-shipping -n amazon --timeout=120s
