apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-search
  namespace: amazon
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ms-search
  template:
    metadata:
      labels:
        app: ms-search
    spec:
      containers:
        - name: ms-search
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/amazon-ms-search:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8091
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: amazon-secrets
                  key: DB_HOST
            - name: DB_NAME
              value: searchdb
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: amazon-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: JAVA_OPTS
              value: "-Xmx256m"
          resources:
            requests:
              memory: "128Mi"
              cpu: "200m"
            limits:
              memory: "256Mi"
              cpu: "600m"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-search
  namespace: amazon
spec:
  selector:
    app: ms-search
  ports:
    - port: 8091
      targetPort: 8091
  type: ClusterIP
